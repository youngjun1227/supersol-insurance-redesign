# 신한 슈퍼쏠 보험 탭 UI/UX 리디자인

## 저장소
- GitHub: https://github.com/youngjun1227/supersol-insurance-redesign (public, main 브랜치). 팀원은 collaborator로 초대받아 clone → Claude Code로 열면 이 파일을 자동으로 읽음.
- 커밋은 사용자가 요청할 때. 캡처 PNG도 포함(총 ~13MB).
- **커밋 메시지에 `Co-Authored-By: Claude …` / `Generated with Claude Code` 절대 넣지 말 것 (사용자 지시, 2026-08-17).** 이 레포는 사람만 기여자로 남긴다. `.claude/settings.json`의 `includeCoAuthoredBy:false` + `.githooks/commit-msg`가 강제함. clone한 팀원은 `git config core.hooksPath .githooks` 한 번 실행.

## 프로젝트 한 줄 요약
신한라이프 슈퍼쏠(SOL) 앱의 **보험 탭만** 대상으로, 기존 컴포넌트 스타일은 유지한 채 **재배치·구조·흐름을 개선**하는 UI/UX 리디자인. 산출물은 Figma MCP로 제작.

## 범위 (절대 잊지 말 것)
- ✅ 대상: **보험 영역 A+B** — A. 금융 탭›보험(내 보험 관리·서비스) + B. 상품 탭›보험(상품 탐색·가입, 신한은행/신한라이프 세그먼트, 상품 목록·상세)
- ❌ 비대상: 홈, 계좌, 이체, 카드 등 보험 탭 외 영역 — 요청받아도 범위 밖임을 먼저 알릴 것

## 리서치 브리프 규칙 (2026-08-17 브리프 v2 — 반드시 따를 것)
원문: `docs/디자인브리프_v2_2026-08-17_팀-아이디어-4개.md` (v1은 대체됨). **사용자(영준)가 팀장.** 팀 SOL:VE 4인, 디자인은 영준+팀원 2명(Figma 초보). 근거 원문은 별도 리서치 저장소, 요약은 브리프 §8.
- **팀이 8/5에 정한 아이디어 4개는 전부 그린다** (우선순위는 8/25 회의). 1 카테고리 통일해 위로 / 2 연령·보유상품 맞춤 테마 / 3 신한카드 결제 감지→청구 알림 / 4 보장에너지 수치. §3 문제 후보(청구 무스크롤·12개 묶음·입구 분기·용어 툴팁)는 별도 화면 없이 흡수.
- **화면 태그는 [아이디어 N]** (H-### 대신). 할 일 전체: `02_to-be/backlog.md` (순위 없음, 팀장이 분배).
- **8/25 종합 회의 전에는 전부 "가설 기반 시안"이다.** "개선안"·"솔루션"이라는 말을 파일명·프레임명·문서에 쓰지 않는다. Figma 페이지도 `시안 (가설 기반)`.
- **P-### 번호를 만들지 않는다.** P 카드는 8/25 Gate 1에서만 생성. 우리 AS-IS 관찰은 **R-xx(리뷰 메모)** + 걸리는 **H-###** 태그로만 (`02_to-be/problems.md`).
- **화면(프레임)마다 [아이디어 N] 태그**를 이름 끝에 붙인다: 예 `카테고리 탭+회사 칩 [아이디어 1]`. 8/25에 지지 안 된 아이디어 화면은 뺀다(이미 그렸어도, 지우지 말고 보류 섹션).
- **한 문제에 2~3안.** AI는 안을 내는 역할, 고르는 건 팀. 안마다 노트에 가설 H·참고 C 카드 ID·바꾼 것·사용자 관점 기대 효과.
- **표현 규칙:** "사용자는 ~를 불편해한다", "명백히", "확실히" 금지 → "가설: ~", "~할 가능성이 있다".
- **회사 관점 효과 금지:** "신한라이프 노출 증대", "가입 전환" 같은 말을 시안 목표·노트에 쓰지 않는다. 사용자 관점("찾는 상품에 더 빨리 닿는다", "판단 근거가 생긴다")만. 특히 A(H-002)에서 계열사 몰아주기 규제(E-102) 주의.
- **룩앤필은 SuperSOL 그대로.** 구조 변화가 측정 대상. 예뻐서 이긴 건지 구조가 나아서 이긴 건지 갈라야 함 → 우리 토큰·컴포넌트 재사용 원칙과 일치.
- 경쟁 앱(모니모 C-301~306, 토스 C-401~406) 참고하되 베끼지 않는다. 참고 ID를 노트에.
- 목데이터: 실명·실번호 금지. "김신한 / 010-0000-0000". 계정 상태 기본 = **"보유 1~2건 20대(부모 가입분 포함)"** (2026-08-18 설문 Q3·Q4 근거로 변경, 이전 기본은 0건). 0건은 두 번째 상태.
- 시안 이미지를 설문·발표에 쓸 때 "개선안"·팀명 글자 넣지 않음.
- 일정: 와이어프레임 ~8/19 → 최종 UI 8/20~24(팀원2 주도) → 🔒 8/25 회의 → React MVP 8/25~31(팀장) → 🔒 9/11 사용자 테스트 → 9/20 발표. MVP 화면 4~6개(S1 보험 메인 / S2 상품 찾기 / S3 보장 진단 / S4 청구 진입 / S5 상세 선택 / S6 알림 팝업 선택).
- 태스크 분리는 팀장이 직접 함 → 확정되면 `02_to-be/backlog.md` 담당 열에 기록. Figma `시안` 페이지의 T1~T4 섹션은 임시(재구성 예정).

## 디자인 원칙 (제약)
**유지 (기존 슈퍼쏠 그대로):**
- 색상 팔레트 (브랜드 블루, 텍스트/배경/구분선 색)
- 버튼 라운드 수치, 버튼 크기·높이
- 카드 라운드·패딩
- 폰트 크기 스케일, 굵기
- 아이콘 크기, 여백 단위
→ 값은 `00_reference/design-tokens/tokens.md`에 정리. Figma Variables 등록 완료.
- **기준 팔레트 = SOL 계열** (`#005DF9` primary, `#101828`~`#818DA2` 그레이, 버튼 h48·r8). 신한라이프 제공 화면의 `#265BF0` 계열은 기준이 아님 — TO-BE에서 SOL로 통일한다. (결정 기록: `docs/2026-08-17_팔레트-2계열-발견-및-기준-결정.md`, 리뷰 메모 R-01)

**개선 (우리가 바꾸는 것):**
- 정보 구조(IA), 화면 흐름, 요소 배치, 우선순위, 그룹핑
- 인터랙션 패턴, 접근성, 인지 부하
- 컴포넌트 자체를 새로 만들지 않고 **기존 컴포넌트를 재조합**하는 방향

## 3단계 워크플로우
| 단계 | 산출물 | 폴더 | Figma 페이지 |
|---|---|---|---|
| 1. AS-IS | 기존 보험 탭 와이어프레임 + 화면 인벤토리 | `01_as-is/` | `01 AS-IS Wireframe` |
| 2. 시안 (가설 기반) | R-xx 메모 → H별 2~3안 시안 (8/25 후 P 확정분만 '개선안'으로) | `02_to-be/` | `시안 (가설 기반)` |
| 3. Final | 실제 토큰 적용한 최종 UI | `03_final-ui/` | `03 Final UI` |

각 단계는 이전 단계 산출물을 근거로 함. 2단계 시안은 반드시 H-### 태그 + `02_to-be/problems.md`의 R-xx를 근거로 연결할 것.

## 폴더 구조
```
00_reference/        기준 자료
  screenshots/       사용자가 올리는 기존 앱 캡처 (파일명: {NN}_{화면이름}.png)
  design-tokens/     유지할 스타일 값 (tokens.md)
  flows/             기존 화면 흐름 (screen-flow.md)
01_as-is/            AS-IS 화면 인벤토리, Figma 링크, 메모
02_to-be/            problems.md(R-xx 리뷰 메모+H 태그) · tasks.md(T1~T4) · improvements.md(시안↔H 매핑), Figma 링크
03_final-ui/         최종 UI Figma 링크, 메모
docs/                회의록, 리서치, 발표자료
```

## Figma 작업 규칙
- **팀:** 신한라이프 슈퍼쏠 UI/UX 개선 프로젝트 (`team::1670707266483782230`)
- **파일:** `https://www.figma.com/design/4ZvrCenVFT98F0VMIYaAp4/슈퍼SOL-UI-Design` (fileKey `4ZvrCenVFT98F0VMIYaAp4`). 페이지 순서: `📖 시작하기`(31:2) / `00 캡처 원본 (AS-IS)`(5:2) / `🧩 컴포넌트 · 토큰 (공용 – 원본)`(5:6, 컴포넌트 섹션 9:2 🔒) / `현재 UI 와이어프레임`(0:1) / `시안 (가설 기반)`(1:3, 구 '개선 UI 와이어프레임') / `최종 UI 디자인`(5:4). 노드 ID는 `01_as-is/figma-links.md` 참고.
- **`use_figma` 호출 전 반드시 `figma-use` 스킬 로드** (`skill://figma/figma-use/SKILL.md`). 화면 단위 조립은 `figma-generate-design`, 컴포넌트 제작은 `figma-generate-library` 스킬 추가 로드.
- **프레임 규격:** 모바일 390×844 (iPhone 기준). 화면 하나 = 프레임 하나. 이름은 `{NN} {화면이름}` (예: `01 보험홈`).
- **와이어프레임 스타일 (1·2단계):** 회색조(low-fidelity). 배경 #FFFFFF, 박스 #E5E7EB, 텍스트 #111827/#6B7280, 이미지 자리는 대각선 X 박스. 실제 색상은 3단계에서만.
- **레이아웃 축 주의:** VERTICAL 컨테이너에서 폭 고정+높이 hug는 `counterAxisSizingMode="FIXED"` + `primaryAxisSizingMode="AUTO"` (HORIZONTAL은 반대). `combineAsVariants` 후 set에 layoutMode 주면 반드시 양축 `AUTO`로. `createAutoLayout` 프레임은 기본 흰 fill이 깔리므로 `fills=[]` 처리.
- **오토레이아웃 필수.** 절대 좌표로 나열하지 말고 `figma.createAutoLayout()`으로 구조를 잡을 것 (헤더 / 콘텐츠 / 탭바 3단 구성 기본).
- **한 번의 `use_figma`에 논리 작업 10개 이하.** 스켈레톤 먼저 → 섹션별로 채우기 → `screenshot()`으로 검증. 에러 나면 즉시 재시도 금지, 원인 파악 후 수정.
- **모든 호출은 생성/수정한 노드 ID를 return.** 화면별 노드 ID는 해당 단계 `figma-links.md`에 기록해서 다음 세션에서 이어갈 수 있게 함.
- **화면 옆에 주석 프레임**을 둬서 AS-IS엔 "관찰된 문제(P-xx)", TO-BE엔 "적용한 개선(I-xx)"을 표기.
- 폰트: **Inter는 한글 글리프가 없으므로 쓰지 말 것.** 와이어프레임·최종 UI 모두 `Noto Sans KR` (스타일: Regular/Medium/Bold, `Semi Bold` 없음). 신한 전용 서체·Pretendard는 이 환경에 없음(2026-08-17 확인).

## 협업 (팀원: 종광·찬영 — Figma 초보, 맥/윈도우 혼재)
- 컴포넌트 원본 섹션(`9:2`)은 **잠금 상태**. 수정하려면 스크립트에서 `locked=false` → 작업 → 다시 `true`.
- `시안 (가설 기반)` 상단에 템플릿 A/B(`30:751`, `30:794`) — 팀원은 이걸 ⌘D로 복제해서 시작. 삭제하지 말 것.
- `📖 시작하기` 페이지(`31:2`)가 팀원용 가이드. 규칙이 바뀌면 여기도 갱신.
- 팀원이 수동으로 그린 요소는 나중에 인스턴스로 교체하는 리뷰 패스를 둔다 (완벽 사용을 기대하지 않음).
- **팀원 Claude는 자기 슬롯 노드 ID를 시작 프롬프트로 받아야 한다.** CLAUDE.md만으로는 '내가 종광인지 찬영인지 · 어느 슬롯에 그리는지'를 알 수 없음. 사람별 프롬프트는 각 `작업지시_*.md` 상단, 슬롯 ID 표는 `02_to-be/figma-links.md`.
- **팀원도 Claude Max + Figma MCP로 작업함.** 역할: 변수·텍스트 스타일 신규 생성과 원본 섹션 관리는 영준(이 세션)만. 팀원 Claude는 `시안 (가설 기반)` 페이지의 태스크 섹션(T1~T4)에서 화면 조립 + **필요 시 신규 컴포넌트 생성 가능**(기존 검색 → 색상 변수 바인딩 → 기존 텍스트 스타일 → 토큰 규격 → `🆕 신규 컴포넌트 (검토 대기)` 섹션 `33:57`에 두기). 영준이 검토 후 원본 섹션 `9:2`로 이동. 시작 프롬프트·규칙: `docs/팀원용_Claude-Figma-작업-가이드.md`. 이 파일의 규칙이 바뀌면 그 가이드의 프롬프트도 같이 갱신할 것.

## 작업 진행 방식
1. 새 세션 시작 시 이 파일 + `01_as-is/screen-inventory.md` + 현재 단계의 `figma-links.md`를 먼저 읽고 어디까지 됐는지 파악.
2. 캡처가 새로 올라오면 `00_reference/screenshots/`를 확인하고 인벤토리에 추가.
3. Figma에 뭔가 만들면 반드시 노드 ID/링크를 md 파일에 기록.
4. 사용자는 한국어로 소통. 문서·Figma 레이어 이름도 한국어 기본.

## 지금 상태
- [x] 캡처 16장 업로드 완료 → 6개 화면군으로 분류·리네임 (2026-08-17). 인벤토리: `01_as-is/screen-inventory.md`
- [x] **범위 확정: A+B** — A. 금융›보험(서비스) + B. 상품›보험(탐색·가입, 신한은행/신한라이프) 둘 다 리디자인
- [x] 사용자 분류 확인 완료 (04는 02 신한은행›암.건강에서 진입, 06은 04의 상품에서 진입, 05는 03 신한라이프›암에서 진입)
- [x] Figma 파일 URL 기록, 페이지 5개 세팅, 캡처 16장 `00 캡처 원본` 페이지에 섹션별 배치 완료
- [ ] 추가 캡처 예정: 보험계약조회, 보험금청구, 보장에너지 상세, 04 보험료확인, 06 보장내용/유의사항 탭
- [x] 토큰 1차 추출 완료 (`00_reference/design-tokens/tokens.md`) — 팔레트 2계열 발견 → **SOL 계열로 통일 결정** (`docs/2026-08-17_팔레트-2계열-발견-및-기준-결정.md`, R-01)
- [x] Figma Variables(색상 2모드: 와이어프레임/SOL, 크기) + 텍스트 스타일 13개 + 공용 컴포넌트 27개 완료 (`🧩 컴포넌트 · 토큰` 페이지 섹션 `9:2`, ID는 `01_as-is/figma-links.md`)
- [x] 1단계 AS-IS 화면 8개 조립 완료 (01, 02, 02-1, 03, 03-1, 04, 05, 06) + 화면별 P-xx 주석 — 섹션 `23:57`
- [ ] 사용자 검토 → 추가 캡처(계약조회·청구·보장에너지·보험료확인·06 탭) 오면 같은 방식으로 추가
- [x] 브리프 v1 → **v2(팀 아이디어 4개, 전부 그림)** 수신. 백로그 `02_to-be/backlog.md` 작성. 담당 분배는 팀장(영준)이 결정 중
- [x] **분담 확정** — 영준(S1 뼈대·S4·S5·공통 재료) / **종광**(S2·S6) / **찬영**(S3·S1 맞춤). 작업지시: `02_to-be/작업지시_*.md`
- [x] **공통 재료 완료** — 목데이터(`02_to-be/mock-data.md`) · 표기 방침(타사·병원 가명) · 신규 컴포넌트 12개 · 템플릿 C · Figma `시안` 페이지 S1~S6 재구성 · A 결정 초안 5개
- [x] **에셋 방침(D-2) 확정 2026-08-23** — 라인 아이콘 = **Phosphor Icons (MIT)** `아이콘 (Phosphor)` `278:110` **42개**(섹션 `278:111` 🔒). 캡처 픽셀 실측(획 1.5@24·라운드 캡·탭바는 채움)으로 Lucide(획 2.0) 대신 선택.
      **3D 일러스트 = Microsoft Fluent Emoji 3D (MIT)** `3D 일러스트 (Fluent)` `292:3927` **18개**(카테고리 9 + 배너 9, 섹션 `292:3926` 🔒). ~~아이콘 타일로 마감~~·~~unDraw~~ 둘 다 폐기. 마스코트(곰) 재현은 여전히 금지.
      규칙: `03_final-ui/디자인.md` §7·§7-1 · 매핑표: `03_final-ui/아이콘-매핑.md` · 소스: `00_reference/assets/`. **팀원은 플레이스홀더를 Swap → Name 드롭다운.**
- [x] **⛔ 에셋 금기 (필독):** 한국에서 사회적·정치적 상징으로 읽히는 이미지 금지. **노란 리본 = 세월호 추모 상징** — 암 카테고리에 넣었다가 뺐다. 해외 아이콘 세트는 국내 오독 검증을 거친 적이 없으니 고를 때마다 확인. `디자인.md` §9
- [x] **최종 UI 22장 전환 완료 (2026-08-23)** — 페이지 `최종 UI 디자인` `5:4` / 섹션 `📱 최종 UI 화면` `310:607` + `🧱 공통 재료` `296:77`. SOL 모드 고정, 플레이스홀더 0, 하드코딩 색 0, 라이프 계열 색 0. 노드 ID 전체: `03_final-ui/figma-links.md`
- [x] **신한 원형 심볼 확보 2026-08-24** — `00_reference/logo/shc_symbol_ci.png`(트림본 `_trim.png`) → Figma 세트 **`로고 (신한)` `360:7488`** (섹션 `360:7489` 🔒), 변형 3개: `신한 심볼`(48²) · `신한라이프`(86×20) · `신한은행`(74×20). 브랜드 색 `#0046FF` 유지 — SOL primary로 치환 금지. **적용: `🧱 공통 재료` 상품 카드 샘플 2장만** (인스턴스 Swap). ⚠️ `상품 카드` 컴포넌트 원본에는 넣지 않는다 — AS-IS 04의 타사 상품 카드 9장이 같은 컴포넌트를 써서 신한 로고가 타사 카드에 붙는다. ⚠️ `판매사 뱃지`에도 넣지 않는다 — 자사만 로고를 달면 타사(가명 텍스트) 대비 시각적 우대(H-002·E-102)
- [ ] **영준 조달 필요:** 신한라이프·신한은행 **공식 워드마크 CI SVG**(심볼만으로는 계열사 구분이 안 돼서 지금은 심볼+텍스트 락업으로 대체, 캡처 추출 금지). 앱 아이콘은 확보·적용 완료(`00_reference/logo/슈퍼쏠앱아이콘_투명.png` → `332:7737`·`332:7738`·`332:7739`)
- [x] **프로토타입 준비 완료 2026-08-24** — 신규 3장(00 메인홈 `387:7886` · 청구 완료 `386:2481` · S1-14 맞춤 OFF `387:2506`), 스크롤·탭바 고정 10장, **연결 18개**, 보드 레인 5개 재배치. 계획·노드 전체: `03_final-ui/프로토타입_계획_8-25.md`. **시연은 보유 2건 한 사람으로** — 0건 화면은 연결하지 않음
- [x] **시연용 페이지 분리 2026-08-24** — `🎬 프로토타입 (시연용)` `394:2641` (원본 `최종 UI 디자인` `5:4`는 그대로). 연결 18개 재매핑 완료 + S1-13 토글→S1-14에 **Smart Animate 0.45s**. ⚠️ `page.clone()`이 **팝업 딤 0.55 → 1**, **중첩 인스턴스 크기**를 컴포넌트 기본값으로 되돌린다 — 복제 후 반드시 재확인 (§8)
- [ ] **Figma UI에서 손으로 1분:** Prototype 패널 **Device = iPhone 14 & 15 Pro (393×852)** 지정. (시작점 3개는 API로 등록 완료). 스크롤 시 탭바가 안 붙어 있으면 탭바 선택 → Fix position when scrolling
