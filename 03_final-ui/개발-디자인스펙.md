# 슈퍼SOL 보험 탭 MVP — 프론트엔드 디자인 스펙

> **React MVP(8/25~31) 구현 기준 문서.** Figma `최종 UI 디자인`(`5:4`) 화면을 그대로 코드로 옮길 때 이 문서대로 한다.
> 값의 출처: `00_reference/design-tokens/tokens.md`(캡처 픽셀 실측) · `03_final-ui/디자인.md`(확정 규칙, §번호는 그 문서 기준).
> 이 문서는 개발 레포로 복사해 간다. **디자인이 바뀌면 원본(디자인.md)을 고치고 여기에 반영한다** — 여기서만 고치지 않는다.

---

## 0. 전제

| 항목 | 값 |
|---|---|
| 기준 뷰포트 | **393 × 852** (iPhone 14/15 Pro). 1pt = 1px(CSS px) |
| 레이아웃 | 모바일 고정. `max-width: 393px` 중앙 정렬, 밖은 회색 여백 (데스크톱에서 열어도 폰 프레임처럼) |
| 높이 | `100dvh` 사용 — **`100vh` 금지** (iOS Safari 하단 바) |
| 폰트 | **Pretendard** (✅ 2026-08-25 팀장 확정 — 웹폰트, 신한체와 시각적으로 가장 가까움). Figma는 Noto Sans KR이었으므로 ±α 차이는 허용. fallback: `Pretendard, "Noto Sans KR", -apple-system, sans-serif` |
| 홈 화면 추가 | `manifest.json` + `apple-mobile-web-app-capable` — 주소창 없는 전체화면. 앱 아이콘: `슈퍼쏠앱아이콘_투명.png` |
| 상태바 | 실기기 상태바가 그대로 보이므로 **화면 안에 상태바를 그리지 않는다** (Figma 프레임의 9:41 상태바는 시뮬레이션용이었음). standalone 모드에선 `viewport-fit=cover` + `env(safe-area-inset-top)` 패딩 |

**한 줄 원칙 (§0): 룩앤필은 슈퍼SOL 그대로. 바꾸는 건 구조·배치·흐름이지 생김새가 아니다.**
9/11 테스트가 기존 앱 vs MVP 비교라서, 여기 값에서 벗어나면 측정이 오염된다. **"조금 더 예쁘게" 손대지 말 것.**

---

## 1. 색 토큰 (CSS 변수)

SOL 계열로 통일 (R-01 결정). 그대로 복사해서 쓴다:

```css
:root {
  /* Primary */
  --primary:            #005DF9;  /* 버튼·링크·탭 선택·카운트·토글 On·체크 On */
  --on-primary:         #FFFFFF;

  /* Text */
  --text-primary:       #101828;  /* 제목·선택 탭·상품명 */
  --text-secondary:     #344054;  /* 카드 제목·리스트 항목·본문 */
  --text-tertiary:      #475467;  /* 비선택 탭·키-값 라벨 */
  --text-caption:       #667085;  /* 부제·설명·푸터·비선택 상단탭 */
  --text-disabled:      #818DA2;  /* 탭바 비선택·chevron·비활성 */
  --text-black:         #000000;  /* 페이지 큰 제목("금융") */
  --text-link:          #005DF9;

  /* Background */
  --bg-page:            #F0F4FA;  /* 메인 페이지 배경만 (상단 그라데이션 #EDF1F8 →) */
  --bg-surface:         #FFFFFF;  /* 카드·탭바·서브 페이지·시트 */
  --bg-tint:            #F4F6FE;  /* 틴트 카드(유틸·안내·서류 박스)·2분할 버튼 */
  --bg-tint-alt:        #F8F9FC;  /* 칩 비선택·히어로·빈 상태 박스 */
  --bg-segment-track:   #E3E7EE;  /* 세그먼트 트랙·토글 Off */
  --bg-chip-selected:   #344054;  /* 필터 칩 선택 */
  --bg-tint-btn:        #ECF0FE;  /* 2차(틴트) 버튼·아이콘 버튼·스텝퍼 번호 원 */

  /* Border */
  --border-default:     #E4E7EB;  /* 카드 테두리·구분선·체크박스 Off */
  --border-light:       #EAECF0;  /* 카테고리 아이콘 박스·옅은 선 */
  --border-strong:      #182230;  /* 키-값 표 상단 굵은 선 */

  /* Accent — 스탯 아이콘 포인트만. 다른 데 금지 */
  --accent-yellow:      #FEB201;

  /* Overlay */
  --dim: rgba(16, 24, 40, 0.40);        /* 팝업·시트 딤 — #101828 40% (남색 계열, 검정 아님) */
  --shadow-card:  0 2px 8px rgba(16, 24, 40, 0.06);   /* 카드 스택 위층 전용 */
  --shadow-float: 0 4px 16px rgba(16, 24, 40, 0.08);  /* 탭바·푸시 배너·팝업 시트 전용 */
}
```

**플랫폼 예외** (우리 토큰 밖이지만 실물 재현이라 허용): 카카오톡 노랑 `#FAE100`, iOS 알림 카드 `rgba(255,255,255,0.92)`. **신한 CI 블루 `#0046FF`** — 로고 전용, primary로 치환 금지.

### 배경 규칙 (§3 확정)

| 화면 | 배경 |
|---|---|
| S1 보험 메인 | `--bg-page` (상단은 `#EDF1F8`→`#F0F4FA` 그라데이션) |
| S2 상품 목록 | 상단(칩·세그먼트 영역) `--bg-page` → 목록부터 `--bg-surface` |
| 그 밖의 전부 (S3·S4·S5·S6·내 보험) | `--bg-surface` 흰색 |

---

## 2. 타이포그래피

13단계 스케일. 줄높이 기본 **150%**. **중간값(17·19·22 외 크기) 새로 만들기 금지.**

| 토큰 | 크기/굵기 | 용도 |
|---|---|---|
| `display` | 28 / 700 | 상품 상세 상품명 · 온보딩 헤드라인 · 청구 제목 |
| `h1` | **22** / 700 | 페이지 제목("금융"·"모든상품") — 24 아님, 8/24 실측 보정됨 |
| `h2` | 20 / 700 | 섹션 제목 · 카드 제목 · 현황 카드 수치 · 팝업 제목 |
| `h3` | 18 / 700 | 카드 소제목 · 묶음 제목("조회·계약") |
| `body-lg-bold` | 16 / 700 | 버튼 글자 · 상품명 · 체크 항목 강조 |
| `body-lg-medium` | 16 / 500 | 리스트 항목 |
| `body-lg-regular` | 16 / 400 | 본문 · **상단 탭 비선택** (Medium 아님, 8/24 보정) |
| `body` | 15 / 400 | 배너 문구 · 키-값 라벨 · 빈 상태 안내 |
| `body-sm` | 14 / 400 | 상품 설명 · 카드 본문 · 서브텍스트 |
| `body-sm-medium` | 14 / 500 | 그리드 메뉴 라벨 · 서류 박스 라벨 |
| `caption` | 13 / 400 | 부제 · 한계 문구 · "왜 보이는지" · 푸터 |
| `caption-medium` | 13 / 500 | 텍스트 링크("기준 바꾸기") · 뱃지 |
| `label` | 12 / 500 | 하단 탭바 라벨 · 작은 뱃지 |

Pretendard 굵기 매핑: 400 Regular / 500 Medium / 700 Bold. (Semi Bold 600은 쓰지 않는다 — Figma 원본에 없던 굵기.)

---

## 3. 라운드 · 크기 · 간격

### radius
```css
--r-btn: 8px;        /* 인라인 버튼·2분할 버튼 */
--r-btn-lg: 16px;    /* 하단 고정 CTA만 */
--r-card-sm: 12px;   /* 소개 카드·카테고리 아이콘 박스·안내 박스·서류 박스 */
--r-card: 16px;      /* 상품 카드·묶음 카드·시트 내부 박스 */
--r-card-xl: 20px;   /* 배너·유틸·인사이트·헤드라인 카드 */
--r-card-lg: 24px;   /* 홈 큰 카드·바텀시트 상단·푸시 알림 카드 */
--r-pill: 999px;     /* 칩·탭바·뱃지·세그먼트 */
--r-check: 4px;      /* 체크박스 */
```
**이 8개 외 라운드 금지** (6·10·14 등).

### 크기
| 항목 | 값 |
|---|---|
| 헤더 | h 56 |
| 상단 텍스트 탭 행 | h 46 · 선택 언더라인 2px |
| 버튼 인라인 / 하단 CTA | h **48** r8 / h **56** r16 |
| 검색 인풋 | h 56 r16 |
| 필터 칩 | h **34** pill |
| 세그먼트 | h **48** r12 (안쪽 알약 r8) |
| 하단 탭바 | **363 × 62** 플로팅 pill · 좌우 여백 15 · 아이콘 22 |
| 하단 고정 CTA 영역 | h 102 (상단 패딩 12 + 버튼 56 + 여유) |
| 아이콘 | 24(인라인) · 32(리스트/그리드) · 44(카테고리 박스 r12) · 64(스탯 원) |
| 리스트 행 | h 60 (서브텍스트 있으면 77) |
| 토글 | 51 × 31 (iOS 표준) |
| 체크박스 | 20 r4 |

### 간격 (4·8·12·16·20·24·32만 사용)
| 어디 | 값 |
|---|---|
| 화면 좌우 여백 | **20** — 단, S1 메인 큰 카드 영역은 **12** (카드 폭 369) |
| 카드 내부 패딩 | 20 (틴트 박스는 14~16) |
| 카드 사이 | 12 (메인) · 16 (서브 화면) |
| 버튼 나란히 gap | 8 |
| 섹션 제목 ↔ 내용 | 16 |
| 행 안 아이콘 ↔ 텍스트 | 12 |
| 본문 하단 여백 | 탭바 있는 화면 **90** / 하단 CTA 있는 화면 **126** |

---

## 4. 공통 레이아웃 셸 (`<AppShell>`)

모든 화면 = **[헤더] + [스크롤 본문] + [탭바 또는 하단 CTA]** 3단.

```
┌─────────────────────────┐
│ Header (h56, sticky)     │ ← 제목형: h1 22 Bold #000 / 서브형: ← 뒤로 + 20 Bold
│ (상단 탭 행 h46, 옵션)     │ ← S1·S2 계열만
├─────────────────────────┤
│ 본문 (overflow-y: auto)  │ ← 배경은 §1 배경 규칙
│                          │
├─────────────────────────┤
│ TabBar 363×62 플로팅 pill │ ← position: fixed, bottom: 12+safe-area, 중앙
│  또는 BottomCTA h102 흰 배경│ ← 화면당 둘 중 하나만
└─────────────────────────┘
```

- **탭바**: 흰 배경 + `--shadow-float`. 탭 5개(홈·금융·상품·혜택·주식). 선택 = **채움 아이콘 22 + 라벨 12/500, 색만 `--primary`** / 비선택 = 같은 아이콘 `--text-disabled`. **선택 원 같은 건 없다** (8/23 정정).
- **하단 CTA**: `position: fixed` 흰 배경 h102, 버튼 h56 r16 폭 353.
- 본문 스크롤 시 헤더는 sticky, 탭바·CTA는 fixed — Figma 프로토타입의 "고정 요소"와 동일 동작.

---

## 5. 컴포넌트 카탈로그

Figma 공용 컴포넌트 27 + 신규 12를 React로 옮긴 것. 이름은 제안값 — 바꿔도 되지만 스펙은 지킬 것.

### 버튼

| 컴포넌트 | 스펙 |
|---|---|
| `<Button variant="primary">` | bg `--primary` · 글자 흰 16/700 · 인라인 h48 r8 / `size="lg"` h56 r16(하단 CTA만) |
| `<Button variant="tint">` | bg `--bg-tint-btn` · 글자 `--primary` 16/500 · 크기 위와 동일. **아웃라인 버튼은 없다** — SOL 2차 버튼은 틴트 채움 (실측 확정) |
| `<SplitButton>` (2분할) | 2 × (173×78) r16 · bg `--bg-tint` · 아이콘 24 + 라벨 16/700 · gap 8 — S1 "보험금 청구 / 보험 찾기" |
| `<IconButton>` | 56×60 r12 bg `--bg-tint-btn` |

### 내비게이션·필터

| 컴포넌트 | 스펙 |
|---|---|
| `<Header>` | h56. 제목형: h1 22/700 `--text-black` + 우측 아이콘(검색·알림 등 24) / 서브형: 뒤로 화살표 + 제목 20/700 |
| `<TopTabs>` | h46 · 16px. 선택: `--text-primary` 500 + 언더라인 2px `--primary` / 비선택: `--text-caption` **400** |
| `<TabBar>` | §4 참고 |
| `<Chip>` | h34 pill · 14/500. 선택: bg `#344054` 글자 흰 / 기본: bg `--bg-tint-alt` 테두리 `--border-default` 글자 `--text-secondary`. 칩 행은 가로 스크롤(`overflow-x`, 스크롤바 숨김) + 우측 펼침(∨) 고정 |
| `<Segment>` | h48 r12 · 트랙 `--bg-segment-track` · 선택 흰 알약 r8 · 글자 15 |
| `<Badge>` | pill h24 · 12/500. 자사: `--primary` 채움 흰 글자 / 타사: 테두리 `--border-default` 글자 `--text-secondary` |
| `<SeeAllRow>` (모두 보기) | 폭 100% h45 bg `--bg-surface` p12/20 · "N개 모두 보기" 14/500 `--text-secondary` + chevron |

### 카드

| 컴포넌트 | 스펙 |
|---|---|
| `<HomeCard>` (홈 큰 카드) | 폭 369 r24 흰 p20~24. 스택 위층에만 `--shadow-card` |
| `<StatusCard>` (현황) | r24 흰 p20 · 제목 20/700 + chevron · 수치 라벨 13 caption / 값 20/700. 상태: 보유 0건 / 1~2건 |
| `<PersonalCard>` (맞춤) | r24 bg `--bg-tint` p20 · 제목 20/700 + 본문 14 + "왜 보이는지" 13 caption + 링크 13/500 primary |
| `<ProductCard>` | 폭 353 r16 테두리 `--border-default` p20. 자사 = 신한 로고 락업 / 타사 = 가명 텍스트("A생명") — **타사에 로고 금지** |
| `<UtilCard>` | r20 bg `--bg-tint` · 2행 + 구분선 |
| `<BannerCard>` | h80 r20 bg `--bg-tint` · 3D 아이콘 32~48 + 문구 15 |
| `<InsightCard>` (S3) | 353 r20 흰 테두리 `--border-default` p14/16 · 헤드(항목명 14/500 caption + 뱃지) / 문장 15 `--text-primary` / 보조 수치 13 caption |
| `<HeadlineCard>` (S3 상단) | 353 r20 bg `--bg-tint` p20 · 제목 18/700 + 부제 14 `--text-secondary` |

### 리스트·행

| 컴포넌트 | 스펙 |
|---|---|
| `<ListRow>` | h60/77 · 아이콘 24~32 + 제목 16/500 `--text-secondary` + 서브 14 caption + chevron `--text-disabled` |
| `<ProductRow>` | 회사명(또는 뱃지) + 상품명 16/500 `--text-primary` + 설명 14 |
| `<GridMenuItem>` | 아이콘 **3D PNG 24** + 라벨 14/400 한 줄 — S1 서비스 그리드 (8/25부터 3D, §6 문법) |
| `<KeyValueRow>` | 라벨 15 `--text-tertiary` / 값 15 `--text-primary` 우측 · 표 상단 `--border-strong` 2px |
| `<StatIcon>` | 원 64 흰 · 아이콘(+ `--accent-yellow` 포인트) · 라벨 13 caption · 값 16/700 — S6 스탯 3열 |
| `<CategoryIcon>` | 박스 44 r12 bg `--border-light` + 3D/라인 아이콘 + 라벨 16/500 |
| `<Stepper>` (절차) | 번호 원 28 bg `--bg-tint-btn` 글자 14/500 · 단계명 16/700 · 설명 14 caption · 행 h72 |
| `<Checklist>` | 체크 20 r4 (On `--primary` / Off 테두리 `--border-default`) + 항목 16 |
| `<Toggle>` | 51×31 · On `--primary` / Off `--bg-segment-track` — iOS 스타일 |
| `<TermTooltip>` | [무배당 ?] 인라인 행 → 탭하면 말풍선 bg `#344054` 글자 흰 13 r8 **오버레이(absolute) — 콘텐츠를 밀지 않음** |

### 오버레이

| 컴포넌트 | 스펙 |
|---|---|
| `<BottomSheet>` | 딤 `--dim` · 시트 흰 상단 r24 p16~32 · X 24 우상단 · 아래에서 올라오는 트랜지션(transform translateY, ~0.35s ease-out) |
| `<CenterPopup>` (S4-A) | 딤 `--dim` · 카드 r24 중앙 · 3D 일러스트 96 중앙 · 제목 20/700 중앙 · 본문 15 중앙 · 서류 박스 bg `--bg-tint` r12 |
| 푸시 배너 (S5-D) | 상단에서 내려오는 iOS 배너 재현 · 카드 흰 92% r24 + `--shadow-float` |

### 화면 전환

Figma 프로토타입에 걸어둔 규칙 그대로:
- 화면 이동 = 오른쪽에서 밀려 들어옴 (PUSH LEFT 0.3s)
- 시트·모달 = 아래에서 올라옴 (0.35s)
- 뒤로 = 역방향
- React에선 CSS transition 또는 Framer Motion 하나로 통일. 라이브러리 남발 금지.

---

## 6. 아이콘 · 에셋

### 라인 아이콘 = Phosphor (MIT) — npm으로

```bash
npm i @phosphor-icons/react
```
- 기본 **`weight="regular"`** (획 1.5@24 — 실측으로 고른 값이므로 bold/light 금지)
- **하단 탭바만 `weight="fill"`**
- 색은 `color` prop에 CSS 변수
- Figma `Name` ↔ Phosphor 컴포넌트 매핑: `03_final-ui/아이콘-매핑.md` (43개)

### 3D 일러스트 = Fluent Emoji 3D (MIT) — PNG 복사

- 소스: 디자인 레포 `00_reference/assets/fluent-3d/*.png` (32종, 256²) → 개발 레포 `public/assets/3d/`로 스크립트 동기화 (WebP 96² 변환 권장)
- 목록·이름: `00_reference/assets/fluent-3d/README.md`

### 로고 = 신한 원형 심볼

- 소스: `00_reference/logo/shc_symbol_ci_trim.png` — CI 블루 `#0046FF` **유지**
- 계열사 표기 = 심볼 + 텍스트 락업("신한라이프"·"신한은행")
- ⚠️ **타사 카드·판매사 뱃지에 로고 금지** — 자사만 로고 달면 계열사 우대(H-002·E-102)

### 행 아이콘 문법 (2026-08-25 확정 — 어느 아이콘을 쓸지 헷갈릴 때)

| 자리 | 아이콘 |
|---|---|
| **보유 계약 행** (내 보험 목록) | 신한 심볼 24 |
| **콘텐츠 행** (서비스 그리드 · 추천 상품 · 비어 있는 보장 · 카테고리 · 배너) | Fluent 3D 24~96 |
| **UI 크롬** (버튼 안 · 헤더 · chevron · 닫기 · 검색) | Phosphor 라인 |
| 하단 탭바 | Phosphor Fill |
| S2 상품 리스트 아이템 | Phosphor 라인 유지 (8/25 논의 전까지) |

---

## 7. 데이터 · 상태

- **목데이터 단일 소스: `02_to-be/mock-data.md`** → `src/data/mock.ts`로 옮김. 임의 값 만들지 말 것.
- 계정 상태 2벌: **상태 B "보유 2건 20대" = 기본** / 상태 A "0건" = 두 번째. `?state=A|B` 쿼리로 전환(리빌드 없이).
- 고정 값: 김신한 · 010-0000-0000 · 총자산 9,250,000원 · 월 보험료 122,400원(홈) · 35,000원/32,000원(보험 메인) · 보장 10항목 가상값 · 타사=가명(A생명·D생명) · 병원=가명.
- **실명·실번호·실제 상품명(타사) 절대 금지.**
- 에이전트 대화(S3-F)는 **프리셋 고정 응답** — 실제 LLM 붙이지 않는다 (테스트 통제 변수).

---

## 8. 개발 시 금지 목록 (§9 번역)

- ❌ 토큰에 없는 색·라운드·폰트 크기 — "가까운 값"이 아니라 **정확히 토큰 값**
- ❌ 그림자 임의 추가 — `--shadow-card`(스택 위층) · `--shadow-float`(탭바·푸시·시트) 두 개뿐
- ❌ 라이프 계열 색 `#265BF0` `#3668F6` `#111726` `#495365`
- ❌ 빨강·주황으로 "부족" 강조 (가입 압박 화법)
- ❌ 노란 리본 등 한국 사회적 상징으로 읽히는 이미지 (세월호 — 실제로 한 번 걸러냄)
- ❌ 실제 앱 에셋 추출·모사, 신한 마스코트(곰) 재현
- ❌ UI 문구에 "개선안"·"솔루션"·팀명 · 회사 관점 문구("노출 증대"·"가입 전환")
- ❌ CSS 프레임워크의 기본 팔레트(Tailwind blue-500 등)로 대충 매핑 — 반드시 §1 hex

## 9. 화면당 검수 (D-6 번역)

- [ ] 색: DevTools로 찍었을 때 §1 토큰 외 hex 0개
- [ ] 라운드·크기: §3 값 중 하나
- [ ] 텍스트: §2 스케일 13개 중 하나 (중간 크기 0)
- [ ] 배경: §1 배경 규칙과 일치
- [ ] 첫 뷰포트(852px) 안에 핵심 행동 (S1: 2분할 버튼 / S2: 상품 3개 / S3: 첫 항목 / S4: 서류 박스)
- [ ] 목데이터 값 §7과 일치
- [ ] Figma 해당 화면과 나란히 놓고 육안 대조

---

## 10. 화면 목록 (Figma → React 대상)

> ⚠️ 최종 목록은 **8/25 회의 후 확정** — 아이디어 우선순위에 따라 4~6장으로 좁힌다. 아래는 현재 Figma 실물 기준 후보 전체. 노드 ID: `03_final-ui/figma-links.md` (일부 표는 낡음 — 구현 착수 시 Figma 실물 재확인).

| 흐름 | 화면 | Figma 노드 |
|---|---|---|
| 공통 | 00 메인홈 | `387:7886` |
| 아이디어 2 | S1-9 보험 메인(2건, 기본) | `310:608` |
| | S1-8 보험 메인(0건) | `317:1147` |
| | S1-13 기준 시트 on / S1-14 맞춤 OFF | `317:1286` / `387:2506` |
| | S1-7 내 보험 도착 | `317:1084` |
| 아이디어 1 | S2-A/B/C 상품 찾기 3안 → S2-D 선택 후 | `317:7548` `317:7976` `317:8473` / `317:7344` |
| | S6-A 상품 상세+툴팁 | `317:1341` |
| 아이디어 3 | S5-A 알림 설정·동의 | `317:7126` |
| | S5-B 카톡 / S5-C 잠금화면 / S5-D 배너 | `317:7173` `317:7212` `317:7243` |
| | S4-A 진입 팝업 → S4-D 절차 안내 → 청구 완료 | `317:6911` `317:7032` `386:2481` |
| 아이디어 4 (신판) | S3-D 브리핑 → S3-C 상세(2건/0건) → S3-E 항목 상세 → S3-F 에이전트 대화 2종 | 시연용 `540:174` `540:222`/`540:3024` `540:340` `540:372`/`540:397` |
